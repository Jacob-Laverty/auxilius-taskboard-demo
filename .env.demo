# Postgres
POSTGRES_USER=taskboard
POSTGRES_PASSWORD=password
POSTGRES_DB=taskboard

# Backend connection string (host "db" = compose service name)
DATABASE_URL=postgresql://taskboard:password@db:5432/taskboard
PORT=4000

# Frontend -> backend URL
VITE_API_URL=http://localhost:4000
